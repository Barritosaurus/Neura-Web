{
  "scripts": {
    "prepare": "husky install",
    "test": "eslint . --max-warnings=0 && cd ./app && npm run test -- --watchAll=false"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "dependencies": {
    "eslint": "^8.34.0"
  }
}
